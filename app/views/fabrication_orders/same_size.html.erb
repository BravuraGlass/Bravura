<div class="page-header">
  <h1>Edit Product Section Size</h1>
</div>
<h3>Size for <%= @product_sections.map(&:name).join(', ') %>
   <small>edit size</small>
</h3>

<% if @product_section %>
  <%= render :partial => 'product_sections/form_size', locals: { product_section: @product_section, ajax: false } %>
<% end %>
